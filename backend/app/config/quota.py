# Google's real free tier (verified, effective since March 2025): 5,000 free
# Pro-SKU calls/month (Text Search New) and 10,000 free Essentials-SKU
# calls/month (basic Place Details). This app tracks Text Search + Place
# Details in one combined counter, so 5,000 — the smaller of the two real
# pools — is used as the shared cap: it's the highest number that still
# guarantees $0 real cost no matter how the mix of call types falls out.
PLACE_DETAILS_MONTHLY_FREE_LIMIT = 5000
QUOTA_WARNING_THRESHOLD = 4000
QUOTA_BLOCK_THRESHOLD = 5000
