# Base URL the frontend uses to call the backend API.
# Local default, if unset on localhost: http://localhost:8000/api
#
# Vercel must set this to the Render backend API URL:
# VITE_API_BASE=https://your-render-backend.onrender.com/api
VITE_API_BASE=
